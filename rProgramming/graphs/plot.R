bitmap(file = "my.png")

x <- "My fuvking string"
print(x)

x <- c(10, 12, 4, 16, 8)
lbs <- c("US", "US", "AustraLLIA", "Germany", "France")

mypie <- pie(x, labels = lbs, main = "Country Data")

print(mypie)

if (mypie == NULL) {
  print(91828219821)
}
